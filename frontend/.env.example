VITE_API_BASE_URL=http://localhost:5000/api/v1
# API proxy port in dev (must match backend PORT). Use 5001 when running via root "npm run dev"
VITE_API_PORT=5001
# Default company ID for Configurator login (when not passed in request)
VITE_DEFAULT_COMPANY_ID=
VITE_APP_NAME=HRMS Portal 2026
VITE_APP_VERSION=1.0.0
