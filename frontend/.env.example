# Leave empty in production (nginx proxies /api/ to backend)
# Only set for direct backend access without proxy
# VITE_API_BASE_URL=http://localhost:5000/api/v1

# API proxy port in dev (must match backend PORT)
VITE_API_PORT=5000
VITE_APP_NAME=HRMS Portal 2026
VITE_APP_VERSION=1.0.0
